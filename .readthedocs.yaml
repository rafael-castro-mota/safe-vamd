version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

# Specify the path to your Sphinx configuration file
sphinx:
  configuration: docs/source/conf.py  # Adjust path to where conf.py lives

python:
  install:
    - method: pip
      path: .