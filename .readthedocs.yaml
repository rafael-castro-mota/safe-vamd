version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.10"
  commands:
    # Ensure the target output directory exists
    - mkdir -p $READTHEDOCS_OUTPUT/html
    # Copy the entire contents of your generated HTML folder
    - cp -r safe_vamd/docs/build/html/* $READTHEDOCS_OUTPUT/html/